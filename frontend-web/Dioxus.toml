[application]
name = "frontend-web"
default_platform = "web"

[web.app]
title = "Bouedig"

[web.watcher]
reload_html = true
index_on_404 = true

# Forward /api/* from the dev server to the backend started by `just dev-web`.
[[web.proxy]]
backend = "http://127.0.0.1:3000/api"
